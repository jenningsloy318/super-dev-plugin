# Empirical Prototype Report — Phase {{ phase_number }}

> Generated by `prototype-runner` at Stage 6.5. Validates spec design constants
> against representative real input before implementation begins. Catches the
> spec-29 trap: plausible-but-wrong assumptions that pass theoretical review
> but fail when run against real data.

## Constants Under Test

| Constant | Spec Value | Tolerance | Source |
|----------|-----------|-----------|--------|
| {{ constant.name }} | {{ constant.spec_value }} | ±{{ constant.tolerance }} | {{ constant.source_section }} |
| ... | ... | ... | ... |

## Representative Inputs

**Selection rationale:** {{ how_inputs_were_chosen }}

| Input | Source | Notes |
|-------|--------|-------|
| {{ input.name }} | {{ input.path_or_origin }} | {{ input.distinguishing_feature }} |
| ... | ... | ... |

Total: {{ N }} inputs spanning {{ distribution_summary }}.

## Measurement Results

| Input | Constant | Spec | Measured | Δ | Within Tolerance? |
|-------|----------|------|----------|---|-------------------|
| {{ input }} | {{ constant }} | {{ spec_value }} | {{ measured_value }} | {{ delta }} | {{ ✓/✗ }} |
| ... | ... | ... | ... | ... | ... |

### Summary statistics per constant

| Constant | min | median | max | Δmax | Verdict |
|----------|-----|--------|-----|------|---------|
| {{ constant }} | {{ min }} | {{ median }} | {{ max }} | {{ delta_max }} | {{ Pass / Borderline / Fail }} |

## Per-Constant Verdict

### {{ constant.name }}: {{ Pass / Borderline / Fail }}

**Rationale:** {{ why this verdict }}

**Outliers (if any):** {{ describe inputs that produced edge-case values }}

**If borderline:** {{ note for future maintainers about when to revisit }}

**If fail:** {{ describe what kind of input class breaks the spec, recommend pivot }}

---

## Overall Verdict: {{ PASS | PASS-WITH-CAVEATS | FAIL }}

## Recommendation

{{ One of }}:
- **Proceed to Stage 7** — all constants validated, spec is sound.
- **Proceed with caveats** — document borderline cases in the spec's "Known Limitations" section. List which constants might need re-tuning based on production-data observations.
- **Invoke pivot-protocol BEFORE Stage 7** — measured deltas exceed tolerance for {{ which constants }}; the spec's algorithm is built on incorrect assumptions. Recommended alternative approach: {{ describe }}.

## Prototype Source

Location: `{{ spec_directory }}/prototype/`

Files produced:
- `{{ source_file }}` — prototype implementation
- `run-output.txt` — captured stdout from prototype execution
- {{ list any rendered samples, intermediate images, etc. }}

## How to re-run

```bash
{{ exact reproduction command }}
```

## Notes for downstream agents (spec-writer, spec-reviewer)

{{ flag anything spec-writer needs to know — e.g., "AC-N must reflect the
borderline behavior we found at minSize=16" or "the 92% rule should be
replaced with a content-aware approach per pivot-protocol output" }}
